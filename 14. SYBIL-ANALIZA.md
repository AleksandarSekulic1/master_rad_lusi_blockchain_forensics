# Sybil & Bot Network Analysis

Dokumentacija novog, samostalnog modula koji pokušava da prepozna **grupe različitih
adresa sa sinhronizovanim ponašanjem** — situacije kada više RAZLIČITIH adresa u kratkom,
vremenski povezanom periodu pozove isti smart contract i/ili istu funkciju — namerno
odvojeno od Graph/Taint/Pathfinding/Behavioral/DEX Swap/Token Approval stranica. Ovo je
**heuristika**, ne dokaz: nijedan nalaz ovog modula nikada ne tvrdi da označene adrese
pripadaju istoj osobi ili entitetu — svaki odgovor nosi eksplicitan disclaimer koji to
kaže.

> Za implementacioni popis (šta je tačno izgrađeno i u kom fajlu — Log/Audit integracija,
> Chain of Evidence, PDF izveštaj, testovi), vidi **`SYBIL-ANALYSIS-IMPLEMENTATION.md`**.
> Ovaj dokument objašnjava heuristiku samu — kako i zašto radi.

**Sadržaj**

| Deo | Šta pokriva |
|---|---|
| [1. Zašto zasebna analiza](#1-zašto-zasebna-analiza) | odnos prema ostalim analizama |
| [2. Šta znači "isti smart contract"](#2-šta-znači-isti-smart-contract) | ograničenje modela podataka, opciona `function_name` kolona, `address` vs `contract` (§2.1), razlika od Token Approval (§2.2) |
| [3. Heuristika](#3-heuristika) | grupisanje, vremenski lanac, prag broja adresa |
| [4. Confidence/risk skor](#4-confidencerisk-skor) | sastavni delovi skora 0-100 |
| [5. Mere protiv false positive-a](#5-mere-protiv-false-positive-a) | šta NE prijavljuje kao klaster |
| [6. API](#6-api) | rute, parametri, oblik odgovora |
| [7. Demo podaci](#7-demo-podaci) | scenario i tačni brojevi |
| [8. Ograničenja](#8-ograničenja) | pošteno o granicama heuristike |
| [9. Lanac dokaza](#9-lanac-dokaza) | GET vs POST /run, custody, strukturiran `sybil_evidence` dokaz (§9.1), forenzički zaključak (§9.2) |
| [10. Frontend stranica](#10-frontend-stranica) | `/sybil-analysis`, značenje polja (§10.0), live filtriranje (§10.0.1), unakrsni poziv Graf/Taint/Putanje/DEX (§10.1), upis u lanac dokaza (§10.2) |
| [11. PDF izveštaj](#11-pdf-izveštaj) | potpisan izveštaj, isti mehanizam kao DEX Swap/Taint |
| [12. Gde je šta u kodu](#12-gde-je-šta-u-kodu) | putanje |

---

## 1. Zašto zasebna analiza

| | Graph | Taint | DEX Swap | Token Approval | Sybil & Bot Network |
|---|---|---|---|---|---|
| Pitanje | Kako izgleda cela mreža? | Koliki deo sredstava potiče od izvora X? | Da li je par transakcija jedan swap? | Da li postoji rizično ERC-20 odobrenje? | Da li više RAZLIČITIH adresa deluje kao koordinisana mreža/bot? |
| Ulaz | evidencija slučaja | seed čvorovi | evidencija (opciono adresa) | evidencija (opciono adresa) | evidencija (opciono adresa i/ili kontrakt) |
| Rezultat | graf čvorova/grana | % zaprljanosti | lista swap kandidata | lista odobrenja | lista Sybil klastera, sa risk skorom |

Sve analize čitaju istu, već očišćenu evidenciju jednog slučaja, ali ovo je fizički
odvojena stranica/ruta/modul, bez deljenog stanja sa ostalima.

## 2. Šta znači "isti smart contract"

Aplikacija nema ABI dekodiranje (nema `eth_getLogs`, nema keccak256/4byte lookup — ista
situacija kao `token_approval_analysis.py`). Zato je "poziv pametnog ugovora" ovde
aproksimiran kao **"transakcija čiji je `recipient_address` data adresa"** — u ovom
modelu podataka to je nerazlučivo od obične uplate na novčanik. Modul zato:

- Uvek grupiše po `recipient_address` (uvek dostupan, obavezna kolona evidencije).
- Opciono profinjuje grupisanje po deklarisanoj funkciji, ako evidencija ima kolonu
  `function_name` (ili `function`/`method`/`method_name`/`contract_function`) — nijedna
  od ovih nije u `ingestion.COLUMN_ALIASES`, pa prolazi neizmenjena do `combined_frame`,
  isti mehanizam koji `currency`/`event_type` već koriste. Bez te kolone, grupisanje
  radi isključivo po kontraktu.

Ovo je namerno pošteno ograničenje, ne previd — modul nikad ne tvrdi da je adresa
potvrđena kao bajtkod pametnog ugovora (za to bi trebalo `eth_getCode`, van obima v1).

### 2.1 `address` vs. `contract` — ko je pozivao, ko je primio

Dva odvojena parametra (i, na frontendu, dva odvojena filter polja) opisuju dve različite
uloge u istoj transakciji:

| Parametar | Uloga | Znači |
|---|---|---|
| `target_address` (UI: **Adresa**) | pošiljalac | Jedna od adresa koje su POZIVALE kontrakt/funkciju — filtrira na klastere u kojima ta adresa učestvuje, bez obzira koji je kontrakt pozvala. |
| `contract` (UI: **Kontrakt**) | primalac | Adresa koja je PRIMILA pozive — „meta", gde se sinhronizacija desila — filtrira na klastere usmerene baš na tu adresu. |

### 2.2 Razlika u odnosu na Token Approval Analysis

Token Approval Analysis (`token_approval_analysis.py`) razlikuje TAČNE uloge iz posebnih,
opcionih kolona evidencije (`event_type`, `token_address`, `spender_address`) — zna
konkretno ko je **vlasnik (owner)**, ko je **odobreni trošilac (spender)**, i za koji
**token**, jer te uloge evidencija eksplicitno deklariše. Sybil & Bot Network Analysis to
NE radi: njen `contract` je isključivo `recipient_address` (§2 gore), bez ikakve uloge
tipa owner/spender i bez potvrde da je zaista pametan ugovor. Ovo je namerno mnogo
grublja heuristika — ne isti koncept kao Token Approval, čak i kad obe analize gledaju
istu evidenciju.

## 3. Heuristika

`app/analytics/sybil_analysis.py` → `detect_sybil_clusters(transactions, target_address=None, contract=None, time_window_seconds=300, min_addresses=3)`

1. Grupiši sve transakcije po `(recipient_address, function_name)` (funkcija je `None`
   kad kolona ne postoji — svi redovi jednog kontrakta se tad prirodno nađu u istoj
   grupi).
2. Unutar svake grupe, hronološki uparuj u **lance** (`_burst_segments`): svaka dva
   uzastopna reda na najviše `time_window_seconds` razmaka ulaze u isti segment — isti
   pohlepni, hronološki princip kao Prolaz 2 u `dex_swap_analysis.py`. Trajanje segmenta
   MOŽE premašiti `time_window_seconds` kad se više kratkih razmaka nalanči — to se
   iskreno prijavljuje kroz `window_duration_seconds`, ne skriva iza parametra.
3. Segment postaje **kandidat klaster** samo ako sadrži transakcije od **najmanje**
   `min_addresses` RAZLIČITIH adresa pošiljalaca (podrazumevano 3, opseg 2-50). Jedna
   adresa koja više puta poziva isti kontrakt NIJE Sybil signal — to filtrira broj
   različitih adresa, ne broj transakcija.
4. Svaki klaster dobija naziv kontrakta (poznat entitet iz `address_enrichment.
   get_known_entity`, ili poznat/verovatan DEX iz `dex_swap_analysis.classify_dex_node`
   — ponovo iskorišćeno, korisno za demo pseudo-adrese, ili sirova adresa bez tvrdnje o
   identitetu).

## 4. Confidence/risk skor

Skor 0-100, sastavljen iz do četiri komponente (svaka objašnjena u `reasons[]` uz tačan
broj poena), klasifikovan u `none`/`low`/`medium`/`high`/`critical` (isti prag kao
`risk_scoring.py`: ≥80 critical, ≥60 high, ≥35 medium, >0 low):

| Komponenta | Maks. poena | Osnova |
|---|---|---|
| Broj adresa | 40 | `min(40, broj_adresa × 6)` |
| Vremenska zbijenost | 30 | `30 × (1 - prosečan_razmak / time_window_seconds)` |
| Identičan iznos | 15 | ≥2 transakcije istog iznosa (bot/skript obrazac) |
| Ponovljena kohorta | 15 | adrese iz klastera koje se pojavljuju i u drugom klasteru |

## 5. Mere protiv false positive-a

1. **Prag broja RAZLIČITIH adresa** (§3.3) — jedna aktivna adresa nikad ne formira
   klaster, bez obzira koliko puta poziva kontrakt.
2. **Vremenski lanac, ne fiksni prozor od početka** — aktivnost razdvojena velikim
   razmakom (npr. nekoliko sati) se NE spaja u jedan klaster, čak i ako bi ukupan broj
   adresa preko oba dela zadovoljio prag.
3. **Grupisanje po funkciji kad je dostupno** — dve različite funkcije na istom
   kontraktu se ne mešaju u isti klaster.
4. **Adresa/kontrakt se traži tačnim poklapanjem** (case-sensitive) — ista konvencija
   kao Pathfinding/Behavioral/DEX Swap.
5. **Disclaimer se nikad ne izostavlja** — ni u jednom odgovoru, ni u jednom prikazu.

## 6. API

```
GET  /api/v1/cases/{case_id}/sybil-analysis?address=<opciono>&contract=<opciono>&evidence=<opciono>&time_window_seconds=<opciono, 10-3600>&min_addresses=<opciono, 2-50>
POST /api/v1/cases/{case_id}/sybil-analysis/run   (isto telo + opcioni custody)
```

GET je pasivna, read-only varijanta (bez lanca dokaza — vidi §9). Odgovor:

```json
{
  "case_id": "46ae7f91db9b",
  "evidence": null,
  "target_address": null,
  "contract": null,
  "time_window_seconds": 300,
  "min_addresses": 3,
  "total_clusters": 1,
  "addresses_flagged": 5,
  "function_data_available": true,
  "clusters": [
    {
      "cluster_id": "SYBIL-1",
      "contract_address": "0xAirdropClaimContract",
      "contract_name": "0xAirdropClaimContract",
      "contract_match_basis": null,
      "function_name": "claimAirdrop",
      "address_count": 5,
      "addresses": ["0xBotWallet1", "0xBotWallet2", "0xBotWallet3", "0xBotWallet4", "0xBotWallet5"],
      "activity_count": 5,
      "window_start": "2026-09-01T10:00:00+00:00",
      "window_end": "2026-09-01T10:01:05+00:00",
      "window_duration_seconds": 65,
      "avg_gap_seconds": 16.25,
      "max_gap_seconds_observed": 17,
      "modal_amount": 0.0001,
      "modal_amount_count": 5,
      "identical_amount_ratio": 1.0,
      "repeated_address_count": 3,
      "risk_score": 88,
      "risk_level": "critical",
      "reasons": ["...", "..."],
      "transactions": [ { "sender_address": "0xBotWallet1", "amount": 0.0001, "timestamp": "...", "tx_hash": "0xsyb0001", "function_name": "claimAirdrop" } ]
    }
  ],
  "disclaimer": "Sybil & Bot Network Analysis je heuristika ... NIKADA ne predstavlja dokaz da navedene adrese pripadaju istoj osobi ili entitetu. ...",
  "generated_at": "2026-09-18T20:00:00+00:00"
}
```

## 7. Demo podaci

`backend/scripts/seed_demo_sybil_evidence.py` dodaje `demo_sybil_analysis.csv` (13 redova,
četiri namerno izolovane grupe) u isti deljeni demo slučaj (`46ae7f91db9b`):

| Grupa | Adrese | Rezultat | Zašto |
|---|---|---|---|
| 1 | 5 adresa poziva `claimAirdrop` na istom kontraktu, ~65s ukupno | **Klaster, critical (88)** | prag adresa ispunjen, identičan iznos, zbijeno vreme |
| 2 | 2 adrese na istom kontraktu | Ništa | ispod praga od 3 adrese |
| 3 | 3 adrese, ali razmaknute satima | Ništa | van vremenskog prozora |
| 4 | 3 od istih 5 adresa iz Grupe 1, drugi kontrakt (`mint`) | **Klaster, high (76)**, `repeated_address_count=3` | ista kohorta se ponavlja — signal pojačan |

Pokretanjem `python scripts/seed_demo_sybil_evidence.py`, pa `GET
/cases/46ae7f91db9b/sybil-analysis?evidence=<stored_name>` — tačno `total_clusters: 2,
addresses_flagged: 5` (Grupa 4 ne dodaje nove adrese, samo ponovo koristi tri iz Grupe 1).

**Zašto baš ovaj fajl u „Prikaz transakcija", nikad „Sve transakcije (kombinovano)":**
algoritam grupiše SVE transakcije u izabranom prikazu po paru (kontrakt, funkcija), bez
obzira odakle potiču. Deljeni demo slučaj (`46ae7f91db9b`) sadrži i 430 pravih
transakcija Ronin Bridge hakera (`GRAPH.md` / `ETHEREUM-DEMO-TESTIRANJE.md` §9) — ta
adresa u stvarnosti prima uplate od desetina različitih adresa u kratkim periodima, pa
ista heuristika tu dodatno prijavljuje oko 13 nepovezanih klastera. Rezultat: nad
kombinovanom evidencijom dobija se **15** klastera umesto **2** iz tabele iznad — ne
greška, nego stvarna, neplanirana ilustracija da heuristika hvata SVAKU sinhronizovanu
konvergenciju ka jednoj adresi, ne samo namerno-dizajnirane demo primere. Za tačne,
ponovljive brojeve, evidencija mora ostati ograničena na `demo_sybil_analysis.csv`.

Automatski testovi:
```bash
python -m pytest backend/tests/test_sybil_analysis.py backend/tests/test_sybil_analysis_route.py -v
```

## 8. Ograničenja

Pošteno, po istom principu kao ostale analize u ovom projektu:

- **Nema potvrde da je `recipient_address` zaista pametan ugovor** (nema `eth_getCode`
  poziv) — bilo koja adresa koja prima sinhronizovanu aktivnost od više pošiljalaca može
  biti označena, uključujući legitiman deljeni novčanik (npr. depozitna adresa berze).
- **`function_name` je opciona kolona i retko popunjena** u ručno pripremljenoj
  evidenciji — kad nedostaje, grupisanje radi samo po kontraktu.
- **Legitimni, nekoordinisani obrasci mogu izgledati identično**: javna prodaja,
  popularan airdrop, viralna kampanja — sve to može proizvesti "više različitih adresa,
  isti kontrakt, kratak period". Risk skor je signal za dalju istragu, ne presuda.
- **Nema poređenja sa poznatim botnet listama ni on-chain heuristikama** (npr. redosled
  finansiranja novčanika iz istog izvora) — v1 gleda isključivo vreme + kontrakt/funkcija
  + iznos u samoj evidenciji.
- **Bez PDF izveštaja i bez Graph integracije** u v1 — read-only pregled i deliberatno
  pokretanje (§9) su implementirani; potpisan izveštaj i overlay na Graf stranici nisu
  (mogu se dodati po istom obrascu kao DEX Swap Analysis, §11/§9 tog dokumenta).

## 9. Lanac dokaza

Isti obrazac kao DEX Swap/Behavioral: dva endpoint-a, namerno razdvojena.

| Ruta | Ko je zove | Custody |
|---|---|---|
| `GET /cases/{id}/sybil-analysis` | pasivan pregled | Nikad |
| `POST /cases/{id}/sybil-analysis/run` | Sybil Analysis stranicino „ANALIZIRAJ" dugme | Uvek — `custody` u telu zahteva |

`run_case_sybil_analysis` gradi `per_evidence_frames`/`combined_frame` na isti način kao
`run_case_dex_swap_analysis`, i kad `custody` stigne, poziva nepromenjen
`record_custody_access()`. Audit log dobija akciju `sybil_analysis_run` (Aktivnost log,
grupa `analysis`, ikonica 👥) sa `custody_recorded` i brojem upisanih transakcija/fajlova.

### 9.1 Strukturiran dokaz po transakciji — blockchain činjenice vs. heuristički zaključci

Za razliku od DEX Swap/Behavioral (koji upisuju samo generički, isti zapis za svaku
transakciju u opsegu), Sybil & Bot Network Analysis dodatno prilaže **strukturiran
`sybil_evidence` dokaz** na custody zapis SVAKE transakcije koja je upala u OZNAČEN
klaster — isti obrazac koji je Token Approval Analysis već uspostavio za svoje nalaze
(vidi TOKEN-APPROVAL-IMPLEMENTATION.md §18). Kad `custody` stigne, `run_case_sybil_
analysis`:

1. Ponovo pokreće `detect_sybil_clusters` nad **tagovanom** (`combine_frames_with_
   evidence_tag`, `_evidence_stored_name` kolona) i **neisfiltriranom** (`target_address=
   None, contract=None`) evidencijom — svaki klaster koji je run stvarno pronašao u
   celoj skeniranoj evidenciji, ne samo onaj koji odgovara `request.address`/`request.
   contract` filteru prikazanom na ekranu (isti "cela skenirana evidencija je pristupljena"
   princip kao LANAC-DOKAZA.md §2).
2. `service.sybil_custody_enrichment()` gradi mapu `tx_id -> sybil_evidence`, jedan unos
   po transakciji unutar OZNAČENOG klastera (transakcije koje nisu deo nijednog klastera
   ne dobijaju ništa).
3. Ta mapa se prosleđuje `record_custody_access(..., extra_transaction_fields=...)` —
   nepromenjenom, deljenom mehanizmu — pa se dokaz upisuje TAČNO na custody red te jedne
   transakcije, ne na ostale.

Svaki `sybil_evidence` unos je eksplicitno podeljen na **tačno dve** grupe (po zahtevu —
ne tri kao kod Token Approval, jer kod Sybil-a nema jasne "u sredini" kategorije
deterministički-izračunatih-ali-neinterpretiranih polja):

| Grupa | Sadržaj | Izvor |
|---|---|---|
| `blockchain_facts` | `sender_address`, `contract_address` (primalac), `amount`, `timestamp`, `transaction_hash`, `block_number` (opciono, `null` ako evidencija ne deklariše), `function_name` (opciono) | direktno iz reda evidencije — bez tumačenja |
| `heuristic_conclusions` | `cluster_id`, `address_count`, `activity_count`, `window_start/end/duration_seconds`, `identical_amount_ratio`, `repeated_address_count`, `risk_score`, `risk_level`, `reasons[]` | sve što je Sybil heuristika ZAKLJUČILA o klasteru u koji je transakcija svrstana — nikad predstavljeno kao činjenica |

Uz oba, `disclaimer` (isti tekst kao API odgovor). `app.evidence.custody_log.py` je
proširen (generička funkcija, ne nova) da prepoznaje i `sybil_evidence` na isti način na
koji već prepoznaje `token_approval_evidence`:
`custody_chain_for_transaction()` ga vraća u detalju jedne transakcije, a
`list_case_transactions()` dodaje `has_sybil_evidence: bool` u spisak za pregled — vidljivo
na stranici **„Lanac dokaza"** kao bedž **👥 SYBIL** u tabeli, i kao poseban, vizuelno
odvojen panel (ljubičast okvir, dve kolone — Blockchain činjenice / Heuristički zaključci)
IZNAD zvaničnog obrasca kad se otvori ta transakcija — obrazac ostaje pravni dokument o
PRISTUPU dokazu, panel je poseban forenzički NALAZ o njemu, nikad pomešano u isti tekst.

Audit log dobija dodatno polje `sybil_findings_recorded` (broj transakcija koje su dobile
`sybil_evidence` u tom pokretanju), a sam API odgovor `custody_findings_recorded` (isti
broj, vraćen direktno klijentu, bez potrebe da čita audit log).

### 9.2 Automatski forenzički zaključak

Na dnu stranice, posle liste klastera i disclaimer-a, prikazuje se **kratak, automatski
sastavljen forenzički zaključak** (`sybil-analysis.component.ts`'s `forensicConclusion`,
client-side, isti duh kao DEX Swap izveštajev `buildConclusionParagraph`) — sastavljen
**isključivo** od brojeva/polja iz upravo prikazanog rezultata: broj klastera, broj
označenih adresa, najizraženiji klaster (adresa/aktivnost/risk skor), broj kritičnih/
visokorizičnih klastera, i (kad je custody upisan) broj transakcija zabeleženih u lancu
dokaza sa linkom ka „Lanac dokaza". Nijedna rečenica ne izlazi izvan onoga što je `result`
stvarno sadržao — nema nagađanja, nema generičkih fraza nezavisnih od stvarnog nalaza.

## 10. Frontend stranica

Ruta `/sybil-analysis`, link **„Sybil & Bot mreže"** u glavnom meniju. Isti
case/evidencija birač kao ostale stranice analize, pa dugme **ANALIZIRAJ**, koje otvara
isti deljeni dijalog razloga pristupa i potpisa pre poziva backend-a (§9). Rezultat:
kartica po klasteru sa risk bedžom (Low/Medium/High/Critical, obojeno), brojem
adresa/aktivnosti, vremenskim prozorom, listom adresa (čipovi), i razvijenim detaljima
(razlozi skora + tabela transakcija). Disclaimer i automatski forenzički zaključak
(§9.2) se NE prikazuju na ekranu (namerno — stranica ostaje pregledna) — oba i dalje
postoje u PDF izveštaju (§11).

### 10.0 Napredna podešavanja — šta znače polja

Sakrivena iza **„▸ Napredna podešavanja"** (podrazumevano zatvoreno, da osnovni tok bude
samo ANALIZIRAJ):

| Polje | Uloga | Znači |
|---|---|---|
| **Adresa** | pošiljalac | Jedna od adresa koje su POZIVALE kontrakt/funkciju — filtrira na klastere u kojima ta adresa učestvuje, bez obzira koji je kontrakt pozvala. Padajuća lista („Adrese iz slučaja", `caseAddresses`) nudi svaki čvor iz grafa — ili slobodan unos. |
| **Kontrakt** | primalac | Adresa koja je PRIMILA pozive — „meta", gde se sinhronizacija desila. Padajuća lista („Kontrakti iz slučaja", `caseContracts`) nudi samo adrese koje su BAR JEDNOM bile primalac neke transakcije (`link.target` u grafu) — namerno UŽA od `caseAddresses`, jer bi ponuda adrese koja nikad nije primalac garantovano vratila 404 (backend-ov `contract` filter traži tačno to). Vidi §2.1/§2.2 za razliku u odnosu na Token Approval-ov owner/spender/token model. |
| **Vremenski prozor (s)** | parametar detekcije | Maksimalan dozvoljen razmak IZMEĐU DVA UZASTOPNA poziva da bi ušli u isti klaster (§3) — nije „ukupno trajanje"; ukupno trajanje klastera (`window_duration_seconds`) može biti duže ako se više kratkih razmaka nalanči jedan na drugi. |
| **Min. broj adresa** | parametar detekcije | Najmanji broj RAZLIČITIH adresa da bi klaster uopšte bio prijavljen — jedna adresa koja više puta zove isti kontrakt nikad nije klaster, koliko god puta pozvala. |

Oba tekstualna polja imaju dugme **×** za brzo pražnjenje, i koriste HTML `<datalist>`
(a ne poseban `<select>`) — jedno polje odjednom radi i kao slobodan unos i kao padajuća
lista predloga, bez dva odvojena kontrolna elementa za istu vrednost.

### 10.0.1 Prvo ANALIZIRAJ je uvek najšire moguće; sužavanje ide uživo, bez potpisa

Prvi (potpisani) klik na ANALIZIRAJ **uvek** poziva `POST .../sybil-analysis/run` sa
`address=null, contract=null, time_window_seconds=3600, min_addresses=2` — najšira moguća
kombinacija — bez obzira šta trenutno piše u „Napredna podešavanja". Cilj: jedan potpisan
pristup evidenciji hvata SVE klastere koje algoritam uopšte može naći (i sve što bi
`sybil_custody_enrichment`, §9.1, moglo ikad označiti), umesto da svaka promena parametra
zahteva novi potpis. Posle uspešnog odgovora, polja se postave na te iste vrednosti (2,
3600), da nikad ne lažu o obimu trenutno prikazanog rezultata.

Svaka SLEDEĆA promena Adrese/Kontrakta/Vremenskog prozora/Min. broja adresa (debounce
400ms, `filterChange$`) poziva **pasivnu** `GET /cases/{id}/sybil-analysis` rutu — istu
evidenciju koju je prvi potpisani poziv već pristupio, samo drugačije prikazanu — bez
novog dijaloga. `custody_findings_recorded` prikazan na stranici i dalje odražava broj iz
PRVOG, potpisanog poziva (`lastCustodyFindingsRecorded`), ne trenutno filtriran prikaz.

### 10.1 Forenzički pregled — unakrsni poziv postojećih analiza

Za svaki klaster, dugme **„Forenzički pregled (Graf/Taint/Putanje/DEX)"** automatski
poziva **POSTOJEĆE** analize, ograničene na adrese tog klastera, i prikazuje samo
najvažnije nalaze — kratak forenzički pregled, ne pretrpana stranica sa punim izveštajima
četiri odvojene analize:

| Analiza | Poziv (isti, nepromenjen backend) | Šta se izvlači |
|---|---|---|
| Graf + Taint | `POST /cases/{id}/analytics/run`, `seed_addresses` = adrese klastera | Najviši `risk_score`; adrese na crnoj listi; **wallet-clustering grupa deljena između ≥2 adrese klastera** (nezavisan signal od same Sybil heuristike — strukturalni, ne vremenski); najviši `taint_percentage` (od izvora unutar klastera); taint na samom kontraktu; broj adresa označenih kao chain-hopping |
| Pretraga putanja | `POST /cases/{id}/pathfinding` × 2 (specific_address između prve dve adrese klastera; nearest_cex od prve adrese) | Da li postoji DIREKTNA putanja (broj skokova) između dve označene adrese; najbliža poznata berza od predstavničke adrese |
| DEX Swap Analiza | `GET /cases/{id}/dex-swap-analysis` (case-wide), filtrirano na adrese klastera | Broj adresa klastera sa detektovanim swap-om (Detected/Potential), koji DEX kontrakti su korišćeni (mogući zajednički punkt unovčavanja) |

Sve tri grupe poziva idu **paralelno** (`forkJoin`) i **bez custody-ja** — ovo je
orijentacioni, pasivan unakrsni pregled (ista logika kao Graf stranicin automatski
pregled pri izboru evidencije, ili DEX Swap overlay), NE nova deliberatna radnja upisana
u lanac dokaza. Panel to eksplicitno kaže; ako analitičar želi formalan, potpisan nalaz
iz bilo koje od te četiri analize, i dalje mora da je pokrene na njenoj sopstvenoj
stranici. Neuspeh jednog dela (npr. Pathfinding) ne blokira prikaz ostala dva — svaki
poziv ima sopstveni `catchError`, panel prikazuje „Nedostupno" samo za taj deo.

Pathfinding koristi **usmerenu** BFS (`bfs_shortest_path`, nepromenjen) — „nije
pronađena" znači da nema direktnog toka sredstava u tom pravcu u ovoj evidenciji, **ne
isključuje** zajedničko finansiranje iz istog izvora uzvodno (za to bi trebala pretraga
zajedničkog pretka, van obima ove verzije) — panel to izričito napominje.

Poziva se **na zahtev** — dugme po klasteru (`runForensicOverview`), ili dugme
**„Forenzički pregled — svi klasteri"** (`runAllForensicOverviews`) koje isto radi za
SVE klastere odjednom, preskačući one koji već imaju učitan pregled (bez duplog
pozivanja pri ponovnom kliku). Ni jedno ni drugo ne piše u lanac dokaza — za to, §10.2.

### 10.2 „Upiši u lanac dokaza" — isti pregled, ali potpisan

Dugme **„🖋 Upiši u lanac dokaza"** (pored „Forenzički pregled — svi klasteri") otvara
DRUGI, poseban custody dijalog (isti `CustodyAccessDialogComponent`, `confirmForensicCustodyAndRecord`).
Na potvrdu, JEDNIM potpisom poziva **deliberatne** (custody-prihvatajuće) varijante istih
analiza za SVE klastere odjednom — ne orijentacioni pregled, nego stvaran, potpisan
pristup koji svaka ciljna analiza sama upisuje u SVOJ custody_log/audit_log, tačno kao da
je analitičar otišao na svaku stranicu posebno:

| Analiza | Poziv | Obim |
|---|---|---|
| Graf + Taint | `POST /cases/{id}/analytics/run` sa `custody` | **JEDAN** poziv, `seed_addresses` = unija adresa SVIH klastera |
| DEX Swap Analiza | `POST /cases/{id}/dex-swap-analysis/run` sa `custody` | **JEDAN** poziv, cela evidencija (`address=null`) |
| Pretraga putanja | `POST /cases/{id}/pathfinding` sa `custody`, × 2 po klasteru | Po klasteru — svaki ima svoj predstavnički par adresa, ne može se objediniti u jedan poziv |

Graf/Taint i DEX Swap su namerno **JEDAN** poziv za ceo skup, ne po klaster — ponavljanje
istog, evidencijom-širokog skeniranja N puta bi upisalo N identičnih custody redova za
iste transakcije. Pathfinding ostaje po klasteru jer je stvarno različit svaki put
(`fetchClusterPathfinding`, deljena implementacija sa pasivnim pregledom §10.1, razlika
je samo da li se `custody` prosleđuje). Svi pozivi dele ISTI potpis — jedan čin
unakrsne provere, ne četiri odvojena opravdanja.

## 11. PDF izveštaj

Dugme **„Izvezi PDF izveštaj"** na `/sybil-analysis` stranici koristi **isti** potpisan-
izveštaj mehanizam kao DEX Swap/Taint/Pathfinding/Token Approval — `POST /api/v1/
reports/register` registruje kontrolni broj PRE nego što je PDF izgrađen, analitičar
nacrta potpis, a dokument se sastavlja na klijentu (`jspdf`/`jspdf-autotable`). Nijedna
nova backend ruta — `report_type: 'sybil'` je samo nova vrednost slobodnog `string` polja
koje `POST /reports/register` već prihvata.

Sadržaj (jedan dokument, više strana): zaglavlje (case, evidencija, filter, parametri,
disclaimer) → rezime (kartice) → po klasteru: naslov + risk bedž → **Ključni dokazi**
(tabela transakcija — blockchain činjenice) → heuristički zaključak klastera (`reasons[]`)
→ **Forenzički pregled** (Graph/Taint/Pathfinding/DEX, kad je pokrenut za taj klaster pre
izvoza — u suprotnom, izveštaj to iskreno kaže) → **Lanac dokaza** (broj upisanih nalaza)
→ **Završni forenzički zaključak** (isti tekst kao §9.2) → Metodologija i ograničenja →
potpis/pečat/kontrolni broj. Puna specifikacija sadržaja i otiska sadržaja:
**`SYBIL-ANALYSIS-IMPLEMENTATION.md` §5**.

## 12. Gde je šta u kodu

| Šta | Fajl |
|---|---|
| Algoritam | `backend/app/analytics/sybil_analysis.py` (`detect_sybil_clusters`) |
| Ruta | `backend/app/features/case_sybil_analysis/router.py` |
| Model zahteva | `backend/app/features/case_sybil_analysis/models.py` |
| Lanac dokaza — strukturiran dokaz (§9.1) | `backend/app/features/case_sybil_analysis/service.py` (`combine_frames_with_evidence_tag`, `sybil_custody_enrichment`) — isti obrazac kao `case_token_approval_analysis/service.py` |
| Prepoznavanje `sybil_evidence` u lancu dokaza | `backend/app/evidence/custody_log.py` (`custody_chain_for_transaction`, `list_case_transactions`) — prošireno, isti mehanizam kao `token_approval_evidence` |
| Testovi | `backend/tests/test_sybil_analysis.py`, `backend/tests/test_sybil_analysis_route.py`, `backend/tests/test_sybil_custody.py` |
| Demo podaci | `backend/scripts/seed_demo_sybil_evidence.py` |
| Frontend stranica | `frontend/src/app/features/sybil-analysis/` |
| Forenzički pregled (§10.1) | `frontend/src/app/features/sybil-analysis/sybil-analysis.component.ts` (`runForensicOverview`, `buildGraphTaintSummary`, `buildDexSummary`) — poziva postojeće `CaseDataApiService.runCaseAnalytics`/`getDexSwapAnalysis` i `PathfindingApiService.findCasePath`, bez ijedne nove backend rute |
| Forenzički zaključak (§9.2) | `frontend/src/app/features/sybil-analysis/sybil-analysis.component.ts` (`forensicConclusion`) |
| Prikaz nalaza u „Lanac dokaza" (§9.1) | `frontend/src/app/features/custody-log/custody-log.component.html`/`.scss` (`.sybil-evidence-panel`, `.finding-badge`) |
| API poziv | `frontend/src/app/features/sybil-analysis/sybil-analysis.api.ts` |
| Tipovi | `frontend/src/app/core/models/shared.models.ts` (`SybilCluster`, `SybilAnalysisResult`, `SybilClusterTransaction`, `SybilCustodyEvidence`), `frontend/src/app/features/custody-log/custody-log.models.ts` (`CustodyChain.sybil_evidence`, `CustodyTransactionSummary.has_sybil_evidence`) |
| Aktivnost log oznake | `backend/app/exports/activity_report.py` + `frontend/src/app/features/activity-log/activity-log.component.ts` — akcija `sybil_analysis_run` |
